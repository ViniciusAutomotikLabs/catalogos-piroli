# gpasi-search — variáveis do container (sem secrets neste arquivo)
REDIS_HOST=tecdoc_redis
REDIS_PORT=6379
REDIS_PASSWORD=
PORT=8080

# Fallback só se a busca NÃO passar fonte=gpasi
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
